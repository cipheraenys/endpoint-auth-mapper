"""Bundled legacy regex rule packs, not verified framework adapters.

Each JSON file inventories route-shaped candidates and auth-looking signals for
the backwards-compatible legacy scanner. Loading a pack proves schema validity,
not framework ownership, auth enforcement, composition, or v2 capability
maturity. See ``docs/reference/rulepack-schema.md`` and the legacy capability
inventory.
"""

from __future__ import annotations
